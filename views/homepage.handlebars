{{#each posts as |post|}}

{{> post-detail post }}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">there are currently no recipe right now !!</h6>
{{/each}}


<div class="card">
    <header class="card-header">
        <p class="card-header-title">
            Live Chat
        </p>
    </header>

    <div class="box">
        <div class="card-content">
            Chat goes here 
        </div>
    </div>

    <footer class="card-footer">
        <input class="input" type="text" placeholder="Text input">
        <button class="button is-dark">Send</button>
    </footer>
</div>